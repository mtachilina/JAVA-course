/**
 * Программа для демонстрации final-методов.
 * 
 * final-метод нельзя переопределить в подклассе.
 * Обычные методы можно переопределять.
 * 
 * @author Тачилина Мария
 * @version 1.0
 * @since 2026-04-24
 */

/**
 * Родительский класс с обычным и final-методом.
 */
class Parent {
    
    /**
     * Обычный метод. Его можно переопределить в подклассе.
     */
    public void normalMethod() {
        System.out.println("Обычный метод");
    }
    
    /**
     * final-метод. Его нельзя переопределить в подклассе.
     * Попытка переопределения вызовет ошибку компиляции.
     */
    public final void finalMethod() {
        System.out.println("Final метод — нельзя переопределить");
    }
}

/**
 * Подкласс, наследующий Parent.
 */
class Child extends Parent {
    
    /**
     * Переопределение обычного метода разрешено.
     */
    @Override
    public void normalMethod() {
        System.out.println("Переопределенный обычный метод");
    }
    
    // Следующий код вызовет ошибку компиляции,
    // так как final-методы нельзя переопределять
    /*
    @Override
    public void finalMethod() {
        System.out.println("Попытка переопределить final метод");
    }
    */
}
